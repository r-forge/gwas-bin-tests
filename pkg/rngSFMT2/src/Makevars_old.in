## Emacs please make this a -*- mode: Makefile; -*-m
##
## Use the R_HOME indirection to support installations of multiple R version

SIMD_FLAGS=@SIMD_FLAGS@
BIG_ENDIAN=@BIG_ENDIAN@
SFMT_NORMAL=1
SFMT_SSE2=@SFMT_SSE2@

#    HAVE_MMX / HAVE_SSE / HAVE_SSE2 / HAVE_SSE3 / HAVE_SSSE3 /
#       HAVE_SSE41 / HAVE_SSE42 / HAVE_AVX


PACKAGE_STRING=@PACKAGE_STRING@

PKG_CPPFLAGS= -ISFMT-src -Iauto  $(SIMD_FLAGS) -DSFMT_NORMAL


ifdef SFMT_SSE2
    PKG_CPPFLAGS += -DSFMT_SSE2
endif


#PKG_LIBS = $(shell $(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()" ) @OPENMP_CFLAGS@

#LIB_SRC_DIR = libsrc
#PKG_CXXFLAGS +=-I$(LIB_SRC_DIR) @OPENMP_CFLAGS@ -DNDEBUG -DUSING_R -Wno-long-long


